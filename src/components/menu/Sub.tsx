import { defineComponent } from 'vue'

const MiSubMenu = defineComponent({
    name: 'MiSubMenu',
    inheritAttrs: false,
    setup() {
        return () => <div></div>
    }
})

export default MiSubMenu
