import { defineComponent } from 'vue'

const MiMenuItem = defineComponent({
    name: 'MiMenuItem',
    inheritAttrs: false,
    setup() {
        return () => <div></div>
    }
})

export default MiMenuItem
