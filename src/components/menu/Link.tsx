import { defineComponent } from 'vue'

const MiMenuItemLink = defineComponent({
    name: 'MiMenuItemLink',
    inheritAttrs: false,
    setup() {
        return () => <div></div>
    }
})

export default MiMenuItemLink
