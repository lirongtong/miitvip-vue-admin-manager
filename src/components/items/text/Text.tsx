import { defineComponent } from 'vue'

const MiItemsText = defineComponent({
    name: 'MiItemsText',
    inheritAttrs: false,
    setup() {
        return () => <div></div>
    }
})

export default MiItemsText
