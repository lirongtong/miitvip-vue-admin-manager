import { defineComponent } from 'vue'

const MiItemsImage = defineComponent({
    name: 'MiItemsImage',
    inheritAttrs: false,
    setup() {
        return () => <div></div>
    }
})

export default MiItemsImage
