import { defineComponent } from 'vue'

const MiNoticeItem = defineComponent({
    name: 'MiNoticeItem',
    inheritAttrs: false,
    setup() {
        return () => <div></div>
    }
})

export default MiNoticeItem
